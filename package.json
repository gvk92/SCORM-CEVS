{
  "name": "scorm-cevs",
  "version": "1.0.0",
  "private": true,
  "description": "SCORM Content Extraction and Versioning System",
  "scripts": {
    "setup": "npm install --ignore-scripts && python -m pip install -r requirements.txt",
    "prestart": "python -m pip install -r requirements.txt",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "start:reload": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "pretest": "python -m pip install -r requirements.txt",
    "test": "pytest -q"
  }
}
